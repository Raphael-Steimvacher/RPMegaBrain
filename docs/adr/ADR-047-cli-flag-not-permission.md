# ADR-047 — Flag de implementação não concede permissão externa

Status: aceita em 11/09/2026.

`--pode-fazer` e `--mode implement` governam apenas o workflow de código. O Gateway ignora essas flags e exige consentimento externo próprio.
