# Quarentena por drift de catálogo

Não habilite tools novas automaticamente. Compare versão, nome, schema, annotations, efeito e capability mapping; confirme que writes proibidos continuam ausentes. Rode a suíte v0.3 e use `connector review-drift` apenas após revisão humana do catálogo atual.
