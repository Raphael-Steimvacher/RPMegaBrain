# Privacy purge

Execute `privacy purge CONNECTOR --profile PERFIL`. O comando remove external refs, receipts e draft intents daquele connector. Revogue também cache e credencial no host/provider quando existirem. A alpha não cria cache de corpo bruto nem administra backups externos.
